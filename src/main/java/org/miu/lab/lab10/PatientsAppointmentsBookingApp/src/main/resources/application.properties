# ─── PAMS — Base Application Properties ───────────────────────────────────────
spring.application.name=PatientsAppointmentsBookingApp

# Default active profile (override on command line or environment variable)
spring.profiles.active=dev

# Disable Spring Boot banner for a cleaner CLI output
# spring.main.banner-mode=off
